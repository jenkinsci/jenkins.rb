source :rubygems

gem "json", "~> 1.5.0"
gem "jenkins-plugin-runtime"
# use this line below if you are developing the whole thing locally
#gem "jenkins-plugin-runtime", :path => "../jenkins-plugin-runtime"

group :development do
  gem "rspec", ">= 2.5.0"
end
